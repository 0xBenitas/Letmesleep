pyinstaller>=6.0

# Transcription vocale (Voxtral)
mistralai>=1.0
sounddevice>=0.4
numpy>=1.24
pynput>=1.7

# Lecture vocale (TTS) — utilise mistralai + sounddevice (deja inclus)

# System tray (optionnel)
pystray>=0.19
Pillow>=10.0
